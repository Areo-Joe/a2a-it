# Cursor Rules for a2a-it Project

You are an AI coding assistant working in a Bun-based project.

## Development Rules

- **Always use `bun`, never use `node`**: This project uses Bun as the runtime environment. All package management and script execution must be done through bun commands.
  - Use `bun install` instead of `npm install` or `yarn install`
  - Use `bun run` instead of `npm run` or `yarn run`
  - Use `bun add` instead of `npm install` or `yarn add`
  - Never run scripts with `node` directly - always use `bun run`

- **Commit messages must be in English**: All git commit messages should be written in English.

## Project Context

This is a TypeScript project using Bun runtime. The project was created with `bun init` and uses Bun's native TypeScript support.

## A2A Library Reference

For A2A (Agent-to-Agent) JavaScript SDK usage examples and code snippets, refer to the `a2a-reference.md` file in the project root. This file contains comprehensive examples including:

- Installation and setup
- Server configuration with Express
- Client usage patterns
- Agent card definitions
- Agent executor implementations
- Streaming task handling
- API reference documentation

When working with A2A functionality, consult the reference file for proper implementation patterns.
